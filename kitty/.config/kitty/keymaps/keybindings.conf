# =============================================================================
#  keybindings.conf — cross-platform bindings via kitty_mod
# =============================================================================
#  kitty_mod is ctrl+shift everywhere. On macOS the native cmd+c/cmd+v still
#  work for copy/paste in addition to these.
# =============================================================================

kitty_mod ctrl+shift
clear_all_shortcuts no

# --- Clipboard ---
map kitty_mod+c copy_to_clipboard
map kitty_mod+v paste_from_clipboard
map cmd+c       copy_to_clipboard
map cmd+v       paste_from_clipboard

# --- Scrolling ---
map kitty_mod+up        scroll_line_up
map kitty_mod+down      scroll_line_down
map kitty_mod+page_up   scroll_page_up
map kitty_mod+page_down scroll_page_down
map kitty_mod+home      scroll_home
map kitty_mod+end       scroll_end
map kitty_mod+g         show_scrollback

# --- Windows (splits) ---
map kitty_mod+enter new_window
map kitty_mod+w     close_window
map kitty_mod+]     next_window
map kitty_mod+[     previous_window
map kitty_mod+f     move_window_forward
map kitty_mod+b     move_window_backward
map kitty_mod+left  neighboring_window left
map kitty_mod+right neighboring_window right
map kitty_mod+l     next_layout

# --- Tabs ---
map kitty_mod+t     new_tab
map kitty_mod+q     close_tab
map kitty_mod+right next_tab
map kitty_mod+left  previous_tab
map kitty_mod+.     move_tab_forward
map kitty_mod+,     move_tab_backward
map kitty_mod+alt+t set_tab_title

# --- Font size ---
map kitty_mod+equal     change_font_size all +1.0
map kitty_mod+minus     change_font_size all -1.0
map kitty_mod+backspace change_font_size all 0
map cmd+equal           change_font_size all +1.0
map cmd+minus           change_font_size all -1.0

# --- Misc ---
map kitty_mod+f5  load_config_file
map cmd+ctrl+,    load_config_file
map kitty_mod+u   kitten unicode_input
map kitty_mod+e   open_url_with_hints
map kitty_mod+delete clear_terminal reset active
